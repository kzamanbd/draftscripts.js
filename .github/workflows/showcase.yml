name: Deploy Next.js site to Pages

on:
    push:
        branches: ['master']
    workflow_dispatch:

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: 'pages'
    cancel-in-progress: false

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Set up Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'

            - name: Install pnpm
              run: npm install -g pnpm

            - name: Install dependencies using pnpm
              run: |
                  pnpm install --frozen-lockfile --filter ./apps/showcase

            - name: Build the project
              run: |
                  cd apps/showcase
                  pnpm run build

            - name: Upload artifact
              uses: actions/upload-pages-artifact@v1
              with:
                  path: ./apps/showcase/out

            - name: Push to GitHub Pages
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  TARGET_REPOSITORY: 'kzamanbd/kzamanbd.github.io'
                  TARGET_BRANCH: 'master'
              run: |
                  cd apps/showcase/out
                  git init
                  git config --global user.name "Kamruzzaman"
                  git config --global user.email "kzamanbn@gmail.com"
                  git add .
                  git commit -m "Deploy Next.js site" || false
                  git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/kzamanbd/kzamanbd.github.io.git
                  git push --force origin HEAD:gh-pages
